{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build":"cd ../client && npm run build",
    "install-client":" cd ../client && npm install",
    "heroku-postbuild":"npm run install- cd../client && npm run build",
    "start": "node server.js",
    "client": "cd ../client && npm start ",
    "dev": "concurrently \"nodemon server.js\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "bootstrap": "^5.1.3",
    "concurrently": "^7.0.0",
    "dotenv": "^11.0.0",
    "express": "^4.17.2",
    "express-validator": "^6.14.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.6",
    "multer": "^1.4.4",
    "react-icons": "^4.3.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
